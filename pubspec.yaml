name: simple_jwt_manager
description: "This is a package that supports authentication using the ROPC defined in RFC 6749."
version: 2.0.1
repository: https://github.com/MasahideMori-SimpleAppli/simple_jwt_manager

environment:
  sdk: ">=2.17.0 <4.0.0"
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  simple_https_service: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  simple_lint: ^2.0.0
